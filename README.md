# Terminal-Web-Extension

This command will send the string to the NodeJS server which will in turn send it to the web extension

```echo "Hello from the terminal!" | websocat ws://localhost:8081
```
